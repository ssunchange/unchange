# CLAUDE.md

このリポジトリで作業するときの申し送り。

## このリポジトリについて

Webディレクターである所有者が、業務のナレッジと成果物をためていく個人リポジトリ。ソフトウェアのプロダクトではないため、ビルドやテストの仕組みは持たない。主な成果物は Markdown のドキュメント。

## 対応の方針

- 会話もドキュメントも日本語で書く
- 所有者はWebディレクター。エンジニア向けの前提を置かず、専門用語を使うときは短く補足する
- Git や GitHub の操作は、何をしているかを平易な言葉で説明してから実行する
- ドキュメントは読み手（半年後の所有者、または引き継ぎ相手）が単体で読んで分かる粒度で書く

## ディレクトリの役割

| 場所 | 役割 |
| --- | --- |
| `docs/templates/` | 案件をまたいで使い回す雛形。特定の案件の情報は書かない |
| `docs/checklists/` | 確認項目。チェックボックス形式で、上から順に潰せる並びにする |
| `projects/` | 案件ごとのフォルダ。1案件1ディレクトリ |
| `projects/_template/` | 新規案件のひな形。ここを直接埋めず、コピーして使う |
| `portfolio/` | 対外的に見せる実績。1実績1ファイル。守秘義務の確認状況を各ファイルに残す |
| `site/` | 公開するポートフォリオサイト。`main` への反映で GitHub Pages に自動公開される |

**このリポジトリは公開設定（public）。**置いたものはすべて誰でも閲覧できる。クライアント名や数値を書くときは、`portfolio/README.md` の匿名化の方針に従う。

`projects/` 配下に置いた案件固有の知見が汎用的だと分かったら、`docs/` 側に雛形として切り出す。

## 作業の進め方

- **1テーマ = 1ブランチ = 1PR**。複数のテーマを1つのPRに混ぜない
- ブランチ名は `claude/<内容>` の形式
- マージは squash merge。履歴を一直線に保つ
- PR の説明には「何を変えたか」だけでなく「なぜ」を書く
- `main` に直接コミットしない

## 書式

- Markdown の見出しは `#` から順に使い、レベルを飛ばさない
- 表は列が3つを超えたら箇条書きに切り替えることを検討する
- ファイル名は英小文字とハイフン（例: `requirements-template.md`）
