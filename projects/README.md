# projects

案件ごとのフォルダを置く場所。1案件につき1ディレクトリ。

新しい案件を始めるときは `_template/` をコピーする。

```
cp -r projects/_template projects/<案件名>
```

ディレクトリ名は英小文字とハイフン（例: `acme-corp-renewal`）。案件が終わっても消さない。次の案件で似た構成を組むときの参照元になる。

案件の中で得た知見が他でも使えると分かったら、`docs/` 側に雛形やチェックリストとして切り出す。
